# Depression_ML
ML algorithm that predicts wether someone is depressed and to what scale using multiple different models and multiple differents datasets. The user can use an interface to check wether or not he/she is depressed.
